# рыночные данные (качаются локально, в репозиторий не кладём)
/data/
*.csv

# артефакты прогонов
trials*.json
*.log
raw_main/

# результаты прогонов и БАЗА ЗНАНИЙ остаются в репозитории: контейнер
# эфемерный, а накопленное знание по гипотезам — главный актив лаборатории
# (knowledge.db, calibration_report.json, search_results*.json, lab_results.json)

__pycache__/
*.pyc
.env
