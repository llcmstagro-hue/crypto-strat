# PROGRESS — журнал конвейера

> Живой лог по этапам. Пишется агентом, читается человеком.
> Правило: сюда пишем ФАКТЫ и честные метрики, а не желаемое.

**Коротко для того, кто вернулся:**

- Построены модули 0–3: загрузчик, бэктест-движок, фильтр анти-оверфита.
- **Фильтр откалиброван дважды** — на синтетике (где известен правильный ответ)
  и на твоих реальных данных. Режет и шум, и заведомую подгонку.
- **Поиск проведён на реальных данных**, H1 и H4, 8 гипотез / 2425 конфигов.
- **Эджа не найдено. Ни одна гипотеза не прошла все 7 барьеров.**
- Ближе всех — **пробой канала на H4** (Дончиан 5/7, Кельтнер 4/7). У них есть
  подписи настоящего эджа: плато параметров, плюс на **всех 5 инструментах**
  включая DOGE, переживают удвоение издержек. Убивает их одно и то же —
  **не хватает данных**: на отложенной выборке остаётся 45–46 сделок, этого
  физически мало для значимости.
- Главный практический вывод: **на H1 издержки съедают всё** (99–125% валовой
  прибыли), на H4 — 23–44%. H1 для этого класса стратегий не жилец.
- Что нужно: **больше истории**. Сейчас 2.5 года (2024-01 … 2026-07), в р.5.4
  закладывалось 4 года с 2022. Это и есть узкое место, а не стратегии.
- **Order block прогнан на H4 отдельно (1/7, отсев)**, но с важным диагнозом:
  направленный эдж у сетапа ЕСТЬ (+0.086% от позиции на сделку, 1063 сделки),
  он просто в 3.5 раза меньше стоимости исполнения. Расширение стопа не
  помогает, а разрушает сетап. Подробности — раздел про order block.

---

## Этап 1 — Данные

**Статус: реальные данные получены от трейдера и приняты в работу.**

### Что пришло и что с ними сделано

5 файлов H1, корзина полная. Формат отличался от ожидаемого — **`ts` в
секундах**, не в миллисекундах. Добавлено автоопределение единиц по величине
(с ~1e9 / мс ~1e12 / мкс ~1e15): перепутать их дорого, ошибка в 1000 раз молча
ломает выравнивание по границе ТФ и начисление funding.

| Инструмент | Баров H1 | Период | Вердикт |
|---|---|---|---|
| BTCUSDT | 22 049 | 2024-01-01 … 2026-07-07 | OK |
| BNBUSDT | 22 049 | 2024-01-01 … 2026-07-07 | OK |
| SOLUSDT | 22 049 | 2024-01-01 … 2026-07-07 | OK |
| DOGEUSDT | 22 050 | 2024-01-01 … 2026-07-07 | OK |
| ETHUSDT | 18 960 | 2024-01-01 … **2026-02-28** | OK после обрезки |

### ⚠️ Найденная проблема: у ETH мёртвый фид с 2026-03

Проверки р.5.6 показали у ETH 239 пропущенных баров (покрытие 98.9%), все
в 2026. Копнул глубже — дело не в пропусках:

```
2026-02   медианный объём 900.0
2026-03   медианный объём   0.70     <-- падение в 1300 раз
2026-04   медианный объём   0.51
2026-05   медианный объём   0.37
```

Цены при этом выглядят абсолютно правдоподобно, OHLC целый, дублей нет — ни
одна из проверок р.5.6 такое не ловит. Это либо мёртвый, либо подменённый фид:
бэктест на таком участке нарисует исполнение, которого в реальности нет.

**Сделано два действия:**
1. ETH обрезан по 2026-03-01 (убрано 2850 баров). Осталось 26 месяцев — этого
   достаточно, инструмент из корзины не выпал.
2. **В `quality_report` добавлена проверка коллапса ликвидности** — сравнение
   месячной медианы объёма с исторической, порог «упал в 20+ раз». Новый
   вердикт `МЁРТВЫЙ ФИД (N баров)`. Этой проверки в р.5.6 не было, и она чуть
   не пропустила поломку в боевой прогон.

### Покрытие режимов рынка — важная оговорка

Окно **2.5 года вместо 4**, заложенных в р.5.4, и **медвежка 2022 в него не
попадает**. Но полный цикл внутри есть:

| Период | BTC | SOL | BNB | Характер |
|---|---|---|---|---|
| 2024 H1–H2 | +48% / +49% | +44% / +28% | +85% / +21% | бык |
| 2025 H1–H2 | +14% / −18% | −20% / −20% | −7% / +31% | распределение, слом |
| 2026 H1 | **−33%** | **−41%** | **−37%** | медвежий рынок |

То есть бык, распределение и настоящая медвежка присутствуют — обкатки
«только на быке» (р.5.4) не произошло. Чего не хватает — глубины выборки,
а не разнообразия режимов.

### H4 собран из H1

Так как поиск на H1 упёрся в издержки (см. Этап 5), H4 агрегирован из H1
функцией `resample_ohlcv`: выравнивание по абсолютной сетке времени
(`ts // tf_ms`, а не по позиции в массиве — иначе после дырки все старшие бары
поедут), неполные группы выбрасываются (бар из 2 часов вместо 4 занижает ATR
и делает стопы нереально узкими). Агрегация сверена поштучно с исходными H1.
Получено 5512 баров H4 на инструмент (4740 у ETH), все с вердиктом OK.

### Чего нет

**Funding history не предоставлена.** Движок работает на плоской ставке
0.01% / 8ч (≈11% годовых) — это близко к историческому среднему по мейджорам,
но для лонгов в периоды высокого фандинга издержка занижена. Заменяется на
историческую подстановкой файла `*_funding.csv` без изменений кода.

### Что с сетью (зафиксировано, актуально для загрузчика)

Среда (Claude Code on the web, изолированный контейнер) выпускает HTTPS только
через агент-прокси с политикой сети. Биржевые хосты закрыты на уровне шлюза:

| Хост | Результат |
|---|---|
| `data.binance.vision:443` | `CONNECT tunnel failed, 403` (policy denial) |
| `fapi.binance.com:443` | `CONNECT tunnel failed, 403` |
| `api.bybit.com:443` | `CONNECT tunnel failed, 403` |

Проверено `curl`-ом и подтверждено в `$HTTPS_PROXY/__agentproxy/status`
(`recentRelayFailures` → `connect_rejected` по всем трём). `pypi.org` открыт,
`raw.githubusercontent.com` открыт — биржи нет.

Проверенная альтернатива: подключённый MCP-сервер Crypto.com отдаёт свечи, но
**максимум 50 баров** за запрос. Для 4 лет по 5 инструментам непригодно. Отброшено.

**Вывод для загрузчика: он написан и готов, но в этой среде не запускается.**
Данные пришли от трейдера напрямую. Проверки качества р.5.6 прогнаны на них
через тот же код загрузчика.

### Данные-заглушки

`crypto_strat/data/synthetic.py` — генератор с двумя режимами:

- `edge` — в цену зашит **настоящий** persistent momentum (скрытая марковская
  цепь режимов с дрейфом);
- `noise` — чистое GBM, структуры нет вообще.

Сила эджа подобрана НАМЕРЕННО СКРОМНОЙ (`trend_strength=0.10` → у пробойной
стратегии exp ≈ +0.11R, PF ≈ 1.17, winrate ≈ 34%). Калибровать фильтр на
гигантском эдже бессмысленно: пропустить PF 15 сумеет и сломанный фильтр.

Бары строятся из 12 под-шагов внутри свечи, поэтому OHLC согласован и стопы
выбиваются реалистично, а не по нарисованным экстремумам.

⚠️ **Метрики стратегий на синтетике не являются доказательством эджа и нигде
в отчётах так не подаются.**

---

## Этап 2 — Движок (модуль 2)

`crypto_strat/engine/` — модульно, читает CSV формата загрузчика.

| Файл | Что делает |
|---|---|
| `indicators.py` | ATR/EMA/SMA/RSI/Дончиан/Кельтнер/свинги — **все causal** |
| `blocks.py` | 10 входных блоков, 7 фильтров (библиотека кубиков р.6) |
| `config.py` | формат гипотезы из блоков, валидация, работа с параметрами |
| `backtest.py` | исполнитель |
| `metrics.py` | метрики в R и в % (отдельно — лимиты CFT) |

### Три вещи, на которых обычно врут, и как они закрыты

**1. Lookahead.** Сигнал возникает на ЗАКРЫТИИ бара `i`, исполнение — не раньше
`i+1`. Это не соглашение, а свойство кода: заявка попадает в очередь с индексом
`signal_bar+1`, и исполнитель не имеет доступа к бару сигнала. Каналы для пробоя
считаются с окном, **исключающим текущий бар** (иначе «пробой максимума, в
который входит сам пробивающий бар» — тавтология). Фильтр старшего ТФ
использует только **закрытые** старшие бары.

**2. Издержки.** Комиссия + проскальзывание берутся **с нотионала на каждой
стороне**; round-trip ≥ 30 bps зашит в конфиг — объект с меньшими издержками
просто не создаётся (`ConfigError`). Плюс funding по факту пересечения
8-часовых границ.

> Ключевой эффект: издержки считаются В ДЕНЬГАХ и лишь потом переводятся в R.
> Стратегия с крошечным стопом получает гигантский нотионал на ту же единицу
> риска — и издержки съедают её ровно как в жизни. Измерено тестом:
> при стопе 2% издержки = **0.15R** на сделку, при стопе 0.15% = **2.00R**.
> Это автоматически убивает «красивые PF на мелком стопе» из р.4.

**3. Внутрибаровая неопределённость.** Задели и стоп, и тейк в одном баре —
считается **стоп**. Всегда. Лимитная заявка исполняется по своей цене или
лучше, стоп-заявка — по своей или хуже (проскок на гэпе против нас).

Прочее: одна позиция на инструмент (следствие правил CFT — нет хеджа, нет
пирамид); сигналы, пришедшие в позицию, считаются и логируются, но не
исполняются; позиция, оставшаяся открытой на конце данных, принудительно
закрывается и попадает в статистику (иначе её убыток «исчезает»).

### Тесты — `tests/test_engine.py` (8/8 проходят)

Главный: **прямая проверка на lookahead**. Ряд обрезается на баре K, бэктест
гоняется на обрезке, и сделки, закрывшиеся до K, обязаны совпасть с полным
рядом **бит в бит** (индексы, цены входа/выхода, R). Любая утечка будущего
немедленно разъедется. Прогоняется на 4 разных конфигурациях блоков
(пробой + htf-фильтр, order block, RSI-2 + MA-фильтр, FVG) — совпало
119 / 191 / 191 / 578 сделок.

Остальные: causality htf-фильтра на трёх обрезках; вход обязан лежать внутри
диапазона своего бара; издержки монотонно ухудшают результат; заниженные
издержки отклоняются конфигом; наказание мелкого стопа; начисление funding
(8ч → 10, 24ч → 30, шорт зеркален); на чистом шуме ожидаемость отрицательна.

> Тест на диапазон бара поймал реальный баг: лимитная заявка при гэпе сквозь
> уровень исполнялась по цене заявки, которая могла оказаться **выше high
> бара** — несуществующая цена. Исправлено на «лимит по своей цене или лучше».

---

## Этап 3 — Фильтр анти-оверфита (модуль 3, ядро)

`crypto_strat/validation/`. Реализованы **все семь** барьеров р.4.
Выживание = ВСЕ семь. «Шесть из семи, ну почти» не существует.

| # | Барьер | Как реализован | Порог |
|---|---|---|---|
| 1 | **OOS** | сплит 70/30 строго по времени; параметры подбираются только на train | test exp > 0, PF ≥ 1.05, ≥50 сделок, сохранение эджа ≥ 30% |
| 2 | **Walk-forward** | катящееся окно train 365д → test 90д, шаг 90д; параметры переподбираются в КАЖДОМ окне | ≥60% окон в плюсе, суммарный OOS > 0, ≥4 окон |
| 3 | **Кросс-инструмент** | ФИКСИРОВАННЫЙ конфиг по всей корзине, без переподбора под инструмент | ≥3 инструментов в плюсе; DOGE не обязан подтверждать, но exp > −0.25R |
| 4 | **Минимум сделок** | пул по корзине | ≥200 всего, ≥30 на инструмент, ≥3 таких инструментов |
| 5 | **Multiple testing** | Deflated Sharpe (Bailey & López de Prado) с **глобальным** счётчиком проб | DSR ≥ 0.95 |
| 6 | **Стабильность** | соседи по сетке ±1 шаг, считается на train (OOS одноразовый, на диагностику не тратится) | медиана соседей ≥50% центра, ≥60% соседей в плюсе |
| 7 | **Издержки** | round-trip ≥30 bps + funding; стоп vs ATR; стресс ×2 издержки | стоп ≥ 0.5 ATR, при удвоении издержек exp > 0 |

Отдельно считаются **красные флаги** р.4 (много правил на малой выборке,
высокий PF на малой выборке, эдж только на одном инструменте, подозрительно
высокий winrate) — они не отсеивают, а попадают в отчёт.

### Решения, которые стоит знать

**Целевая функция подбора параметров — Sharpe по сделкам, не доходность.**
Доходность — прямая дорога к подгонке под целевые 3%/мес (р.0, р.7). PF
взрывается на малых выборках. Sharpe штрафует разброс, то есть меряет
устойчивость. Выборки меньше 30 сделок отбрасываются целиком.

**Счётчик проб глобальный и лежит на диске** (`trials*.json`). Если перебрать
50 гипотез по 20 конфигов, победитель — лучший из 1000, и планка барьера 5
обязана соответствовать 1000, а не 20. Счётчик переживает перезапуски, иначе
после рестарта система занизит планку.

**Сетка параметров объявляется заранее и является частью гипотезы.** Если бы
её можно было молча расширить, увидев результат, счётчик проб потерял бы смысл.

**Ранжирование выживших — по робастности, доходность в score не входит вообще.**
Веса: walk-forward 0.30, кросс-инструмент 0.25, плато параметров 0.20,
сохранение эджа на OOS 0.15, DSR 0.10.

---

## Этап 4 — КАЛИБРОВКА ФИЛЬТРА ✅

`calibrate_filter.py`. Матрица из трёх проверок, у каждой известен правильный ответ.

**Результат: `ФИЛЬТР ОТКАЛИБРОВАН`. Перебрано 1779 конфигов (edge) + 471 (noise).**

| Тест | Что проверяет | Ожидание | Факт |
|---|---|---|---|
| **A. чувствительность** | честная стратегия на данных С эджем | ПРОЙТИ | ✅ **7/7**, робастность 0.943 |
| **B. специфичность** | та же стратегия на ЧИСТОМ ШУМЕ | ОТСЕВ | ✅ отсев (3/7) |
| **C1/C3. подгонка: мелкий стоп** | стоп 0.15%, R/R 4 | ОТСЕВ | ✅ отсев (1/7) |
| **C2/C4. подгонка: 6 фильтров** | узкое «удачное» подмножество | ОТСЕВ | ✅ отсев (1/7) |

### A — честная стратегия прошла (позитивный контроль)

Пробой Дончиана, 3 правила, стоп в ATR:

```
1. OOS        train exp=+0.267R sharpe=+0.129 n=684 -> test exp=+0.328R PF=1.70 n=278
2. Walk-fwd   10/12 окон в плюсе (83%), суммарно OOS exp=+0.313R n=734
3. Кросс      4/4 инструментов в плюсе; DOGE exp=+0.174R n=1005
4. Сделки     4831 всего, 5/5 инструментов с достаточной выборкой
5. DSR        0.995 при 473 пробах (SR +0.167 против планки случайного максимума +0.041)
6. Плато      медиана соседей 93% от центра, 100% соседей в плюсе
7. Издержки   съедают 34% валовой; стоп = 2.60 ATR; при x2 издержках exp=+0.146R
```

Важно: эдж тут скромный (exp +0.27R, PF 1.54) — фильтр видит **реальный
масштаб**, а не только рисованный.

### B — на чистом шуме отсеяна. И вот что здесь самое ценное

```
1. OOS        train exp=+0.013R -> test exp=+0.074R PF=1.13 n=323   <-- ПРОШЁЛ
6. Плато      медиана соседей -19% от центра, 0% соседей в плюсе    <-- убил
2. Walk-fwd   6/12 окон в плюсе (50%), суммарно OOS exp=-0.019R     <-- убил
5. DSR        0.232 при 471 пробе (SR +0.044 против планки +0.082)  <-- убил
7. Издержки   съедают 102% валовой; при x2 издержках exp=-0.126R    <-- убил
```

**На ЧИСТОМ ШУМЕ барьер OOS прошёл.** Лучший из 471 конфига показал на
out-of-sample PF 1.13 на 323 сделках — там, где эджа нет по построению.
Это эмпирическое доказательство того, что **одного OOS категорически
недостаточно**, и почему барьеры 2, 5, 6, 7 не бюрократия. Ровно так и
выглядит «нашли стратегию, она подтвердилась на отложенной выборке».

### C — подгонка зарезана, и по тем барьерам, по которым должна была

**Мелкий стоп** (C1/C3): exp = **−1.047R** и **−1.346R** на сделку. Издержки
составили **423%** валовой прибыли, медиана стопа = **0.22 ATR**. Одного
честного учёта издержек от нотионала хватило, чтобы конструкция стала абсурдной
ещё до статистики.

**Шесть фильтров** (C2/C4): фильтры срезали выборку до **50 и 30 сделок** по
всей корзине. И снова поучительно:

```
1. OOS      train exp=+0.331R n=9 -> test exp=+0.482R PF=1.76 n=4   <-- ПРОШЁЛ (на 4 сделках!)
6. Плато    100% соседей в плюсе                                     <-- ПРОШЁЛ
4. Сделки   50 всего, 0/5 инструментов с выборкой                    <-- убил
2. Walk-fwd 2/12 окон в плюсе (17%)                                  <-- убил
5. DSR      0.660 при 1303 пробах; нужно 49 сделок, есть 4           <-- убил
🚩 флаг: правил 9 (> 6) при 50 сделках
```

PF 1.76 на **четырёх** сделках прошёл и OOS, и проверку плато. Барьер минимума
сделок — не формальность, это единственное, что стоит между такой картинкой и
выводом «стратегия работает».

### A' — order block трейдера: отсев, но это НЕ приговор методологии

Честный OB (прямой порт `smc_order_block_backtest.py`) на синтетике:
1/7 барьеров, exp = −0.385R.

**Так и должно быть, и это ничего не говорит про OB на реальном рынке.**
Синтетический ряд построен как persistent momentum — в нём есть трендовая
структура, которую ловит пробой, и **нет** структуры «импульс → возврат в зону
→ продолжение», на которой живёт order block. Это тест инструмента на данных,
где OB-эджа нет по построению; отрицательный результат ожидаем.

Роль A' в калибровке — служебная: он подтвердил, что блок OB собирается,
исполняется и корректно проходит все барьеры. **Вопрос «есть ли эдж у OB»
решается только на реальных данных.**

### Калибровка ПОВТОРЕНА на реальных данных ✅

`python calibrate_filter.py --data ./data` — прогон по твоим CSV.
Перебрано 968 конфигов (реальные) + 183 (шум).

**Важная поправка в логике: на реальных данных тест A перестаёт быть
критерием.** Смысл теста чувствительности — «честная стратегия на данных, где
эдж ТОЧНО ЕСТЬ, обязана пройти». На реальном рынке никто не знает, есть ли там
эдж, поэтому «не прошла» одинаково объясняется и глухим фильтром, и отсутствием
эджа — различить нечем. Ground truth есть только у синтетики. Поэтому
чувствительность калибруется там, а на реальных данных критериями работают
**B (специфичность)** и **C (анти-подгонка)**. Это зашито в код.

| Тест | Результат на реальных данных |
|---|---|
| **B. специфичность** (шум) | ✅ отсев по 6 барьерам из 7 |
| **C1/C3. мелкий стоп** | ✅ отсев; exp −1.672R и −1.259R, PF 0.22 / 0.36 |
| **C2/C4. шесть фильтров** | ✅ отсев; выборка срезана до 65 и 76 сделок |
| ℹ️ A. честный пробой | отсев (1/7) — справочно, не критерий |

**Вердикт: `ФИЛЬТР ОТКАЛИБРОВАН`.**

Показательная деталь: честный пробой Дончиана на реальном H1 дал exp **−0.069R**
при том, что на чистом шуме — **−0.054R**. То есть по этой метрике реальный
рынок на H1 для простого пробоя практически неотличим от шума. Причина видна
в барьере 7: **издержки съели 173% валовой прибыли**. Валовая была
положительной — её съели комиссии.

## Этап 5 — Веб-поиск гипотез (модуль 1b)

**Статус: поиск проведён на реальных данных, H1 и H4. Выживших нет.**

### Как оценивалось сырьё (р.6.5)

Только два признака: (а) есть ли чёткие механические правила, переводимые в
конфиг р.6; (б) не дубликат ли уже стоящего в очереди. **Обещанная доходность
игнорировалась полностью** — ни одна цифра доходности из источников
(«Sharpe 2.17», «minimal_roi 5%») не перенесена и не повлияла на приоритет.

Ограничение доступа: `arxiv.org`, `papers.ssrn.com`, большинство сайтов
закрыты тем же прокси (403). Открыт `raw.githubusercontent.com` — код
freqtrade читался напрямую. Академические правила восстанавливались по
результатам веб-поиска и общеизвестным формулировкам первоисточников.

### Очередь Фазы 1 — 8 гипотез, 169 конфигов

| Гипотеза | Правил | Комб. | Источник |
|---|---|---|---|
| `acad_tsmom` | 3 | 27 | SSRN 2089463 (Moskowitz/Ooi/Pedersen); SSRN 4675565 (крипто, с издержками) |
| `acad_tsmom_htf` | 4 | 18 | то же + комбинаторика блоков (р.6.5 п.4) |
| `classic_donchian_breakout` | 3 | 27 | Kaufman; система Дончиана/«черепах» |
| `classic_keltner_breakout` | 3 | 18 | Kaufman; волатильностный пробой |
| `classic_ma_cross` | 3 | 18 | Carver, Systematic Trading |
| `oss_bb_rsi_meanrev` | 4 | 27 | github.com/freqtrade/freqtrade-strategies, Strategy002 |
| `oss_bb_meanrev_bare` | 3 | 18 | то же, контроль без фильтра |
| `oss_rsi2_connors` | 4 | 16 | QuantifiedStrategies / StockCharts — RSI(2), Connors |

**Важное преобразование при переносе.** Стратегии freqtrade написаны на 5m и
навешивают 6–8 условий сразу (Strategy002: RSI<30 + stochastic slowk<20 +
close ниже нижней BB + свечной «молот»; Strategy004 — восемь условий). Перенос
«как есть» невозможен: проект не опускается ниже H1, а 7 условий на нашей
выборке — прямой красный флаг р.4, и **наша же калибровка (случай C2) показала,
что такая конструкция умирает на минимуме сделок**. Поэтому из каждой взято
ЯДРО механики (2–3 условия) и перенесено на H1. Если эдж существует только в
конъюнкции семи условий на 5m — он и не переживёт честной валидации.

Собственные сетапы трейдера (order block, FVG, дивергенции) в очередь **не
входят**: по р.6.5 они калибровочный эталон, а не сырьё для поиска.
Форумы и соцсети (источник №5) — Фаза 2, подключаются последними.

Размер очереди выбран сознательно скромным: каждая лишняя комбинация поднимает
планку барьера 5 **для всех гипотез разом**. 169 конфигов на OOS-проходе,
с walk-forward — порядка 2400 проб.

### РЕЗУЛЬТАТ ПОИСКА — H1 (2024-01 … 2026-07)

```
гипотеза                       барьеров  робастн.  сделок    exp,R     PF
acad_tsmom_htf                      2/7     0.511     853   +0.094   1.16
acad_tsmom                          1/7     0.275     718   +0.096   1.12
classic_keltner_breakout            1/7     0.258    1877   +0.006   1.01
classic_donchian_breakout           1/7     0.254    2463   -0.036   0.95
classic_ma_cross                    1/7     0.207    1172   -0.071   0.89
oss_bb_rsi_meanrev                  1/7     0.050     570   -0.246   0.68
oss_bb_meanrev_bare                 1/7     0.000    2266   -0.207   0.72
oss_rsi2_connors                    1/7     0.000    2051   -0.115   0.36
```

**Выживших нет. Убийца на H1 — издержки.** Доля валовой прибыли, съеденная
комиссиями и фандингом:

| Гипотеза | Издержки от валовой | Итог |
|---|---|---|
| classic_donchian_breakout | **125%** | валовая была в плюсе, издержки увели в минус |
| classic_keltner_breakout | **99%** | издержки съели ровно всё |
| acad_tsmom / acad_tsmom_htf | 62% / 61% | остаток +0.09R, но при ×2 издержках уже минус |

Обе TSMOM-гипотезы имеют положительную ожидаемость (+0.094R и +0.096R) —
и **обе уходят в минус при удвоении издержек** (−0.035R, −0.028R). То есть
эдж целиком помещается внутрь запаса по комиссии. Это не эдж, это зазор
в модели исполнения.

Возврат к среднему (`bb_meanrev`, `rsi2_connors`) отрицателен уверенно
(−0.11 … −0.25R, PF 0.36–0.72) — на H1 в этой корзине он мёртв.

### РЕЗУЛЬТАТ ПОИСКА — H4 (мотивированная проверка)

H1 упёрся в издержки → прямая проверка гипотезы «на старшем ТФ на сделку
приходится больше движения при той же комиссии». Счётчик проб **не обнулялся**:
H4 выведен из тех же цен, смещение отбора переносится, планка барьера 5 обязана
быть выше (1728–1857 проб против 517 на H1).

```
гипотеза                       барьеров  робастн.  сделок    exp,R     PF
classic_donchian_breakout           5/7     0.644     690   +0.103   1.21
classic_keltner_breakout            4/7     0.665     711   +0.171   1.36
acad_tsmom_htf                      2/7     0.573     186   +0.206   1.55
acad_tsmom                          2/7     0.572     196   +0.415   1.54
classic_ma_cross                    2/7     0.200     385   +0.048   1.09
oss_rsi2_connors                    1/7     0.100     550   -0.031   0.76
oss_bb_rsi_meanrev                  1/7     0.100     315   -0.170   0.71
oss_bb_meanrev_bare                 1/7     0.079     289   -0.201   0.70
```

**Гипотеза об издержках подтвердилась**: доля издержек упала со 99–125% до
23–44%, и все трендследящие вышли в плюс. **Но выживших всё равно нет.**

#### Ближайший промах: пробой Дончиана на H4 (5/7)

```
✅ 6. Плато        центр +0.260R, медиана соседей 74% от центра, 100% соседей в плюсе
✅ 3. Кросс        4/4 инструментов в плюсе; DOGE +0.109R на 141 сделке
✅ 4. Сделки       690 всего, 5/5 инструментов с достаточной выборкой
✅ 2. Walk-forward 4/6 окон в плюсе (67%), суммарно OOS +0.064R
✅ 7. Издержки     съедают 44%; стоп 2.47 ATR; при ×2 издержках всё ещё +0.034R
❌ 1. OOS          train +0.260R -> test +0.005R (сохранение эджа 2%), n=46
❌ 5. DSR          0.165 при 1728 пробах; сделок для значимости не хватает
```

Это **не похоже на подгонку**. У подгонки не бывает плато параметров, плюса
на всех пяти инструментах разом и устойчивости к удвоению издержек — наша
собственная калибровка (случаи C1–C4) показывает, что подогнанные конструкции
валятся именно на этих барьерах. Здесь они пройдены.

Что не сошлось: **на отложенной выборке эдж обнулился** (+0.005R против
+0.260R на train) при **46 сделках**. Кельтнер — та же история: OOS-плюс
(+0.083R, PF 1.17), но 45 сделок < порога 50.

**Это упор в объём данных, а не приговор стратегии.** 2.5 года H4 дают на
отложенные 30% около 45 сделок на инструменте подбора — на таком объёме
«эдж исчез» и «эджу негде проявиться» неразличимы. С 4+ годами (окно р.5.4
с 2022) OOS-выборка была бы вдвое больше.

#### Отдельно: как барьеры отработали на приманках

`acad_tsmom_htf` на H4 показал на OOS **PF 7.19 при exp +1.231R** — и был
отсеян: сделок 7, красный флаг «эдж есть только на BTCUSDT». `acad_tsmom` —
PF 3.70 на 13 сделках, DSR 0.969 (почти прошёл!), но walk-forward 2/6 и
суммарный OOS −0.401R. Ровно то, ради чего строились барьеры 2, 4 и 5.

### Проверка проводки на синтетике (историческая, до получения данных)

Прогон очереди по синтетике — убедиться, что все блоки собираются, все барьеры
отрабатывают и ничего не падает. **Это НЕ результат поиска.**

```
гипотеза                       барьеров  робастн.  сделок    exp,R     PF  итог
classic_donchian_breakout           7/7     0.924    3694   +0.312   1.62  ВЫЖИЛА
classic_keltner_breakout            7/7     0.920    4356   +0.274   1.54  ВЫЖИЛА
classic_ma_cross                    5/7     0.836     127   +0.497   1.87  отсев
acad_tsmom                          0/7     0.150      44   +1.319   3.03  отсев
acad_tsmom_htf                      0/7     0.000      78   -0.071   0.87  отсев
oss_bb_rsi_meanrev                  0/7     0.000      84   -0.040   0.94  отсев
oss_bb_meanrev_bare                 0/7     0.000     100   -0.275   0.63  отсев
oss_rsi2_connors                    0/7     0.000     198   -0.111   0.51  отсев
```

Что здесь важно и что НЕТ:

- ✅ **Проводка исправна.** Все 8 гипотез породили сделки (44…4356) — ни один
  блок не пустой. Все барьеры отработали.
- ✅ Отдельно приятно: `acad_tsmom` показал **PF 3.03 при exp +1.32R** — и был
  отсеян, потому что сделок 44 < 50. Барьер минимума сделок работает именно
  так, как задумано: красивая метрика на малой выборке не проходит.
- ❌ **«Выжили 2 из 8» НИЧЕГО не значит про рынок.** Синтетика построена как
  persistent momentum, поэтому трендследящие блоки там обязаны быть в плюсе,
  а возврат к среднему (`bb_meanrev`, `rsi2_connors`) обязан быть в минусе.
  Это свойство генератора, а не свойство стратегий.

Счётчик проб в шапке отчёта («гипотез прогнано 16») накопительный и переживает
перезапуски — так и задумано (барьер 5).

---


## Найденные выжившие

**НЕТ. Эджа не найдено.**

Проверено 8 гипотез на двух таймфреймах, суммарно **2425 конфигов**, все
барьеры отработали. Ни одна гипотеза не прошла все семь.

Это **валидный результат** (р.7). Пороги не трогались, метрики не
подкручивались. Единственное, что было сделано после первого отрицательного
результата, — проверка H4, и она была **мотивирована конкретным диагнозом**
(издержки съедают 99–125% валовой на H1), а не желанием «чтобы хоть что-то
прошло». Диагноз подтвердился, но до прохождения фильтра всё равно не хватило.

### Что это значит на практике

1. **H1 для этого класса стратегий закрыт.** При честном round-trip 0.3%
   пробойные и возвратные стратегии на часовиках не окупают исполнение.
   Это ответ на вопрос «а не спуститься ли ниже» — нет, там будет хуже.

2. **H4 живой, но не доказан.** Трендследование (Дончиан, Кельтнер) показывает
   согласованный плюс по всей корзине, плато параметров и устойчивость к
   удвоению издержек. Не хватает единственного — статистики.

3. **Возврат к среднему в этой корзине мёртв** на обоих ТФ (PF 0.36–0.76).
   Формализованные из freqtrade и Connors идеи проверены и отбракованы.

4. **Ограничение — данные, а не идеи.** 2.5 года дают ~45 сделок на отложенной
   выборке. Барьеры OOS и DSR при таком объёме непроходимы почти для любой
   честной стратегии с удержанием в дни.

### Чего это НЕ значит

- Не значит, что эджа нет вообще: проверено 8 гипотез из огромного
  пространства, только Фаза 1 (академия + классика + открытый код).
- Про order block трейдера теперь есть отдельный ответ — см. раздел ниже.
  Коротко: направленный эдж у сетапа ЕСТЬ (+0.086% от позиции на сделку),
  но он в 3.5 раза меньше стоимости исполнения.

---

## Что делать дальше (по приоритету)

1. **Дать больше истории.** Это узкое место №1. Нужен диапазон **2022-01 …
   2026-07** вместо текущего 2024-01. Тогда OOS-выборка на H4 вырастет вдвое,
   и барьеры 1 и 5 станут проходимы для реального эджа. Плюс попадёт медвежка
   2022 — важно по р.5.4. Загрузчик готов:
   ```bash
   python download_data.py --source bulk --tf 1h 4h 1d --start 2022-01 --funding
   ```

2. **Дать funding history** — сейчас плоская ставка 0.01%/8ч. Для удержаний
   в дни (а H4-трендследование именно такое) реальный фандинг может заметно
   изменить нетто, особенно для лонгов.

3. **Разобраться с источником ETH.** Файл сломан с 2026-03 (объём упал
   в 1300 раз). Перекачать; заодно проверить, не тот же ли источник у остальных
   четырёх — там объёмы тоже снижаются, хоть и не катастрофически.

4. **После этого — перезапуск в том же порядке** (импорт присланных CSV —
   `python import_csv.py <папка> --resample 4h 1d`, он сам приводит формат,
   собирает старшие ТФ и прогоняет проверки качества):
   ```bash
   python calibrate_filter.py --data ./data          # B и C обязаны пройти
   python run_search.py --data ./data --tf 4h        # H4 приоритетно
   python run_search.py --data ./data --tf 1d        # D — ещё меньше издержек
   ```
   Счётчик проб (`trials_search.json`) удалить перед прогоном на новых данных:
   поправка на multiple testing считает пробы **по тем же данным**, а это
   будет другой набор.

5. **Только если после этого кто-то пройдёт 7/7** — форвард на демо CFT
   (модуль 4). Целевые 3%/мес и 4% просадки применяются ТАМ, на выходе, а не
   раньше (р.2).

### Не сделано (за рамками поставленных этапов)

Модули 4–7 (форвард на демо CFT, журнал + Монте-Карло, монитор деградации,
аллокатор) не строились. Пороги монитора по р.6.6 должны считаться из
Монте-Карло каждой стратегии — а Монте-Карло нужны живые распределения R,
которых пока нет.

---

## Order block трейдера на H4 — отдельный прогон (по запросу)

**Результат: 1/7 барьеров, отсев. exp −0.132R на 1010 сделках, PF 0.83.**

Прогонялся на общих основаниях, счётчик проб не обнулялся (2425 → 2618 —
планка барьера 5 выше, чем у гипотез поиска).

```
✅ 4. Сделки       1010 всего, 5/5 инструментов с достаточной выборкой
❌ 1. OOS          train -0.080R -> test -0.096R, PF 0.88
❌ 6. Плато        0% соседей в плюсе
❌ 3. Кросс        0/4 инструментов в плюсе
❌ 2. Walk-forward 2/6 окон
❌ 5. DSR          0.061 при 2618 пробах
❌ 7. Издержки     съедают 218% валовой; медиана стопа 0.79 ATR
```

### Но диагноз важнее вердикта: сырой эдж У СЕТАПА ЕСТЬ

Разобрал, что именно убивает. Ожидаемость ДО издержек и ПОСЛЕ:

| ТФ | сделок | до издержек | после | цена издержек | медиана стопа |
|---|---|---|---|---|---|
| H1 | 4046 | **+0.125R** (PF 1.20) | −0.350R | 0.475R / сделку | 0.84 ATR |
| H4 | 1010 | **+0.113R** (PF 1.18) | −0.132R | 0.244R / сделку | 0.78 ATR |

Направленный эдж есть, он согласован на двух ТФ и на 5000 сделок — это не шум.
Убивает его **структура риска**: зона OB — одна свеча, она уже ATR, поэтому
стоп тесный, нотионал на единицу риска огромный, и 0.3% round-trip
превращается в 0.24–0.48R на сделку.

### Проверка прямого следствия: а если стоп шире?

Прогнал тот же вход со стопом в ATR (2.0/2.5/3.0) — **не помогло, 1/7,
exp −0.048R**. Причина видна, если перевести эдж из R в проценты от позиции:

| Вариант | сделок | сырой эдж, % нотионала | нужен round-trip |
|---|---|---|---|
| **стоп по краю зоны (как у трейдера)** | 1063 | **+0.086%** | < 0.09% |
| стоп 2.5 ATR | 469 | −0.319% | безнадёжно |
| стоп 2.5 ATR + трейлинг | 555 | −0.027% | безнадёжно |

**Ключевой вывод: расширение стопа не спасает, а разрушает сетап.** Логика
OB в том, что зона держит; если она пробита — надо быть вне рынка, а не
пересиживать. Родной стоп по краю зоны оказался лучшим из проверенных
вариантов, а не худшим.

### Итоговый ответ по order block

Сетап зарабатывает **0.086% от позиции на сделку** до издержек.
Исполнение стоит **0.30%** round-trip (обязательный минимум р.4, барьер 7).
**Разрыв примерно 3.5×, и он не закрывается настройкой стопа или выхода.**

Честная оговорка в пользу сетапа: вход у OB — ЛИМИТНАЯ заявка в зону, то есть
это maker-исполнение, а не taker. При maker-входе (~0.02%/сторона на Bybit,
без проскальзывания — лимит исполняется по своей цене) и taker-выходе по стопу
реалистичный round-trip ≈ **0.125%**. Это уже сопоставимо с 0.086%, но всё ещё
выше. **То есть даже при самой благоприятной модели исполнения сетап не выходит
в плюс** — не хватает примерно в полтора раза.

Порог 0.3% я НЕ понижал: это guardrail р.4/р.7, и менять его — решение трейдера,
а не моё. Но разделение maker/taker в движке — обоснованное улучшение модели
(сейчас обе стороны считаются одинаково), и на лимитных входах оно даёт более
точную картину. Скажешь — сделаю.

Детали: `order_block_4h.json`, `order_block_atr_4h.json`.

---

## Где лежат детальные результаты

Машиночитаемые отчёты с разбором по каждому барьеру закоммичены в репозиторий
(контейнер эфемерный, а разбираться придётся на свежую голову):

| Файл | Что внутри |
|---|---|
| `calibration_report.json` | калибровка на реальных данных: A/B/C, все барьеры |
| `search_results_1h.json` | поиск на H1: 8 гипотез, заметки по всем барьерам, лучшие конфиги |
| `search_results_4h.json` | то же на H4 |
| `order_block_4h.json` | order block трейдера на H4, все барьеры |
| `order_block_atr_4h.json` | он же со стопом в ATR |

Логи прогонов: `calibration_real.log`, `search_real.log`, `search_real_4h.log`
(в git не кладутся, лежат в контейнере).

---

## Открытые вопросы к трейдеру

1. **Глубина истории.** Готов дать 2022-01 … 2026-07? Это главный блокер:
   без него барьеры OOS и DSR на H4 непроходимы для эджа такого масштаба
   в принципе, сколько гипотез ни перебирай.

2. **Порог DSR 0.95.** После реального прогона видно, что он работает жёстко,
   но осмысленно: `acad_tsmom` на H4 получил DSR 0.969 при 1405 пробах — то
   есть порог пропускает, когда есть основания. Валили его другие барьеры
   (walk-forward 2/6). Считаю, что менять его не нужно. Если ты видишь иначе —
   обсудим явно, тихо крутить не буду.

3. **Maker/taker в движке.** Сейчас обе стороны сделки считаются одинаково.
   Но лимитные входы (order block, FVG, ретест уровня) — это maker, а стопы —
   taker. Разделение даст более точную картину именно для лимитных сетапов.
   Порог 0.3% round-trip при этом трогать не буду: это твой guardrail (р.4/р.7),
   его снижение — твоё решение, а не моё. Делать разделение?

4. **Инструмент для подбора параметров.** Сейчас BTC, остальные четыре —
   независимая проверка. Так и оставить?

5. **Куда пускать риск.** Напоминаю вывод р.1, он не изменился: челлендж
   проваливают размером позиции. Пока фильтр никого не пропустил, вопрос
   сайзинга преждевременный — но когда пропустит, стартовать имеет смысл
   с 0.5%, а не 1%.
