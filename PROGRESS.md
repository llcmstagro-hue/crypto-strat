# PROGRESS — журнал лаборатории

> Живой лог. Правило: сюда пишем ФАКТЫ и честные метрики, а не желаемое.
> Последний прогон: новые классы механизмов (волатильность, межрынок) на H4 и D1,
> глубокая история 2017–2026.

---

# ГЛАВНОЕ

**Достойных стратегий по-прежнему нет.** База знаний — **208 записей**
(104 на H4 + 104 на D1), все прогнаны через все 7 барьеров. Лучший результат
**6/7**, шесть кандидатов, и все шестеро падают на ОДНОМ И ТОМ ЖЕ барьере 5
(поправка на multiple testing).

Пороги не снижались, минимум сделок не снижался, DSR не трогался.

### Главный результат прогона: затухание — не свойство пробоя, а свойство РЫНКА

Проверялась гипотеза, что новые классы механизмов могли не затухнуть так же,
как пробой. **Не подтвердилась.** Спад значим у ВСЕХ проверенных классов:

| гипотеза | класс | 2017–21 | 2022–26 | p(спад случаен) |
|---|---|---|---|---|
| donchian_breakout+filter_htf | пробой | +0.698R (PF 2.33) | +0.164R (PF 1.27) | **0.0005** |
| xmkt_donchian_btc_filter+stop_structure | межрынок | +0.246R (PF 1.77) | +0.037R (PF 1.11) | **0.0048** |
| vol_squeeze_breakout | волатильность | +0.345R (PF 1.60) | +0.009R (PF 1.01) | **0.0139** |
| xmkt_btc_spillover | межрынок | +0.265R (PF 1.47) | +0.052R (PF 1.09) | **0.0200** |
| vol_nr_expansion | волатильность | +0.193R (PF 1.36) | +0.042R (PF 1.07) | **0.0371** |
| vol_ttm_squeeze | волатильность | +0.273R (PF 1.48) | +0.060R (PF 1.10) | **0.0424** |

Ожидаемость упала на **65–97%** независимо от механизма. Это уже не «пробой
выторговали» — это **систематический эдж на крипто-мейджорах в целом сжался**
по мере взросления рынка. Сжатие волатильности, межрыночный лаг и пробой
уровня — механизмы разной природы, а спад у всех один и тот же и статистически
значим.

Практический смысл: искать «ещё один класс механизмов» на ЭТОЙ корзине и ЭТИХ
таймфреймах, скорее всего, бесполезно. Проблема не в том, какой механизм взять,
а в том, что на пяти самых ликвидных монетах простые правила больше не платят.

### Новое: критерий свежести эджа введён и работает

Реализован как обязательное условие достойности наравне с 7 барьерами:
ожидаемость обязана быть положительной И в окне с 2024-01-01, И в окне
с 2025-01-01 (при выборке ≥30 сделок в первом окне). Провал = вердикт
**`historical-only edge`** в базу знаний с этой причиной.

Из 48 кандидатов, дошедших до 5/7 и выше, **9 провалили свежесть** — критерий
реально различает, а не штампует. Но честная оговорка: **ни разу за этот
прогон он не стал РЕШАЮЩИМ**, потому что до 7/7 никто не дошёл, и вердикт
`historical-only edge` (который выносится только при 7/7 + режимы) не сработал
ни разу. Он стоит в конвейере и ждёт первого кандидата, который дойдёт.

Шесть лидеров свежесть прошли — но еле-еле, и в этом видно то же затухание:

| кандидат | вся история | с 2024 | с 2025 |
|---|---|---|---|
| donchian_breakout+filter_htf | +0.424R | +0.244R | **+0.103R** |
| vol_ttm_squeeze | +0.206R | +0.124R | **+0.083R** |
| keltner_breakout+stop_channel | +0.142R | +0.058R | **+0.016R** |
| xmkt_donchian_btc_filter+stop_structure | +0.095R | +0.040R | **+0.022R** |

Эдж в 2025+ в 3–10 раз меньше исторического у каждого.

### Академия против народной приметы: победила примета, но слабо

Проверены ОБЕ версии межрыночного эффекта, обе формализованы честно:

| версия | источник | результат |
|---|---|---|
| **спилловер** (BTC вверх → альты вверх) | DergiPark, NARDL-работы | 4/7, n=771, exp **+0.125R**, PF 1.27 |
| **качели** (крупные предсказывают альты В МИНУС) | SSRN 3465924 Jia/Wu/Yan/Yin | 3/7, n=1940, exp **+0.057R**, PF 1.10 |

Качели на этой корзине не подтвердились: в 2022–26 они уходят в **−0.143R
при PF 0.76**, тогда как спилловер держится слабо положительным.

Это НЕ опровержение статьи. Там изучался широкий срез монет включая мелкие,
на дневных кросс-секционных портфелях; у нас четыре мейджора на H4 — другая
постановка. Честная формулировка: **на этой корзине и этом ТФ эффекта качелей
не видно, слабый положительный спилловер — есть.**

### Что нашли нового по существу

Лучший кандидат прогона — **межрыночный**: `xmkt_donchian_btc_filter+stop_structure`,
робастность **0.817** (выше всех предыдущих). Пробой на альте берётся только
по направлению тренда BTC. 6/7, падает на барьере 5, свежесть и режимы проходит.

Накопленное знание по типам (208 записей, оба ТФ):

| тип | гипотез | ср. барьеров | ср. робастность |
|---|---|---|---|
| **breakout** | 32 | **4.53** | 0.752 |
| trend | 2 | 4.00 | 0.542 |
| **volatility** | 48 | **3.73** | 0.569 |
| **cross-market** | 50 | **3.70** | **0.645** |
| SMC | 34 | 3.03 | 0.486 |
| momentum | 18 | 2.44 | 0.476 |
| mean-reversion | 24 | 1.29 | 0.246 |

Новые классы встали в середину: заметно выше SMC и возврата к среднему, ниже
пробоя. Межрыночный класс — второй по робастности после пробоя.

---

## Что построено (надстройки р.6.7)

| Модуль | Файл | Что делает |
|---|---|---|
| База знаний | `crypto_strat/knowledge/db.py` | SQLite, каждая гипотеза навсегда: источник, правила, тип, вердикт Critic, метрики, режимы, причина, счётчик проб на момент теста |
| Research Score | `crypto_strat/search/score.py` | 0–100 по весам р.6.7, порог 70, ДО дорогого бэктеста |
| Critic | там же | адвокат дьявола: дубликаты, >6 правил, широкая сетка, подсвечный стоп, объёмные блоки без объёма, односторонняя торговля |
| Classifier | там же | тип стратегии (breakout/trend/momentum/mean-reversion/SMC) |
| Evolution | `crypto_strat/search/evolution.py` | 8 именованных мутаций с рыночной логикой у каждой, безопасная версия |
| Режимы | `crypto_strat/validation/regimes.py` | приёмка «держит эдж и в медвежке, и в быке» |
| Свежесть эджа | `crypto_strat/validation/regimes.py` | **обязательное условие**: эдж положителен с 2024 И с 2025, иначе вердикт `historical-only edge` |
| Бумажный форвард | `crypto_strat/forward/monitor.py`, `run_forward.py` | заморозка конфига + Монте-Карло пороги деградации (модуль 6) |
| Блоки: волатильность | `blocks.py` | `squeeze_breakout`, `ttm_squeeze`, `nr_expansion` |
| Блоки: межрынок | `blocks.py` | `ref_momentum`, `ref_seesaw`, фильтр `ref_trend` — читают ВТОРОЙ инструмент |
| Оркестратор | `run_lab.py` | весь конвейер + отчёт |

**Evolution — строго безопасная версия (р.6.7).** Вариант = новый независимый
кандидат: валидность от родителя НЕ наследуется, каждый проходит все 7 барьеров
с нуля, каждый увеличивает счётчик проб и поднимает планку всем остальным.
Функции «взять лучший вариант по истории» в модуле физически нет. Семена для
мутаций выбраны по РАЗНООБРАЗИЮ механизмов, а не по прошлому результату —
выбор семян по результату и был бы ядовитой версией.

**Research Score сработал как задумано:** 4 гипотезы отсеяны ДО бэктеста
(все — дубликаты по отпечатку логики: `donchian+exit_trailing` совпал с базовым
дончианом, у которого трейлинг и так стоял). Это сэкономило не только время,
но и планку барьера 5 для остальных.

---

## Этап 1 — Данные

**H4, 5 инструментов, из main. Качество проверено (р.5.6).**

| Инструмент | Баров | Период | Покрытие | Вердикт |
|---|---|---|---|---|
| BTCUSDT | 19 593 | 2017-08-17 … 2026-07-29 | 99.91% | OK |
| ETHUSDT | 19 596 | 2017-08-17 … 2026-07-29 | 99.93% | OK |
| BNBUSDT | 19 112 | 2017-11-06 … 2026-07-29 | 99.93% | OK |
| DOGEUSDT | 15 484 | 2019-07-05 … 2026-07-29 | 99.99% | OK |
| SOLUSDT | 13 070 | 2020-08-11 … 2026-07-29 | 100.00% | OK |

Пропуски единичные (0–17 баров), дублей нет, OHLC целый, выравнивание по
границе 4h идеальное.

**Сверка с прежними данными:** новый H4 совпал с H4, собранным ранее из H1,
**бит в бит** на всём перекрытии (5512 баров, максимальное расхождение 0.000%).
Источники согласованы.

**Объём пустой** — как ты и предупредил. Обработано:
- `volume` убран из обязательных колонок загрузчика (иначе `dropna` сносил
  весь файл — эту мину я поймал на первом же импорте);
- объёмный фильтр теперь **падает с явной ошибкой** на данных без объёма,
  а не работает молча на нулях: молчаливая маска из NaN-сравнений либо
  пропустила бы всё, либо ничего, и в обоих случаях выглядела бы осмысленно;
- в отчёте качества добавлена колонка «объём: есть/НЕТ»;
- в калибровке объёмный фильтр в подгонке C2/C4 заменён на ценовой `ma_side`,
  число правил (6) сохранено — тест на «много фильтров» остался эквивалентным.

**Покрытие режимов — полное**, чего раньше не хватало:

| год | BTC | ETH | BNB | SOL | DOGE | режим |
|---|---|---|---|---|---|---|
| 2017 | +215% | +138% | +402% | — | — | бык |
| 2018 | **−72%** | **−82%** | −27% | — | — | **медвежка** |
| 2019 | +95% | −2% | +134% | — | −46% | бык |
| 2020 | +300% | +466% | +170% | −48% | +132% | бык |
| 2021 | +58% | +394% | +1248% | +10505% | +3321% | бык |
| 2022 | **−65%** | **−68%** | **−52%** | **−94%** | −59% | **медвежка** |
| 2023 | +156% | +91% | +27% | +920% | +28% | бык |
| 2024 | +121% | +47% | +127% | +84% | +254% | бык |
| 2025 | −7% | −11% | +22% | −35% | −63% | боковик |
| 2026 | −27% | −36% | −34% | −41% | −40% | **медвежка** |

Три медвежьих периода (2018, 2022, 2026), четыре бычьих, один боковик.

**Чего нет:** funding history. Движок работает на плоской ставке 0.01%/8ч.
Для H4-трендследования с удержанием в дни это заметная статья — подставится
из файла `*_funding.csv` без изменений кода.

---

## Этап 2 — Движок: поломки, найденные и закрытые

Помимо инварианта стопа (см. «Главное», п.1), устранено:

**Лимитная заявка при гэпе** исполнялась по цене заявки, которая могла оказаться
**выше high бара** — несуществующая цена. Теперь лимит исполняется по своей цене
ИЛИ ЛУЧШЕ (гэп сквозь заявку = исполнение по открытию), стоп-заявка — по своей
ИЛИ ХУЖЕ.

**Метка выхода `stop`** покрывала и начальный защитный стоп, и подтянутый
трейлингом. Из-за этого в диагностике появлялись «прибыльные стоп-лоссы»
(на самом деле — нормальные трейлинг-выходы). Разведено на `stop` и
`trail_stop`. Профиль лучшего кандидата теперь читается сразу:

```
trail_stop  n=197  средний R  +1.553
stop        n=115  средний R  -1.100
```

Классический трендследящий профиль: winrate 37%, редкие крупные выигрыши.

**Тесты: 9/9.** Главный по-прежнему — обрезка ряда на баре K с побитовым
сравнением уже состоявшихся сделок (ловит любой lookahead). Добавлен тест
инварианта стопа.

---

## Этап 3 — Фильтр: одна методологическая правка

Все 7 барьеров р.4 без изменений порогов. Изменён **смысл N** в барьере 5 —
выношу отдельно, потому что это моё решение и ты можешь его отменить.

Раньше в счётчик проб попадали ВСЕ прогоны, включая внутренние переборы
walk-forward. Их много: 32 окна × 27 конфигов на каждую гипотезу. За ночь это
дало бы N ≈ 34 500 — и барьер стал бы непроходимым по бухгалтерской причине.

Поправка на multiple testing отвечает на вопрос «мы выбрали лучшего из N —
насколько он мог оказаться лучшим случайно». В N входят те конфиги, которые
МОГЛИ стать опубликованным победителем: перебор на train внутри гипотезы плюс
все гипотезы между собой. Конфиг, выбранный в окне 7 walk-forward, меряется на
собственном OOS окна 7 и агрегируется — победителем он не становится никогда.

Теперь считаются **два** числа: `total` (34 565, нагрузка) и `selection`
(1042, пул отбора). Барьер 5 использует `selection`. **Порог DSR 0.95 не
тронут.**

Проверка, что это не поблажка: лучший кандидат получил DSR **0.684** — то есть
даже на скорректированном N он не прошёл. Правка ничего не «спасла».

---

## Этап 4 — Калибровка на глубоком H4 ✅

`ФИЛЬТР ОТКАЛИБРОВАН`. Перебрано 4480 конфигов (реальные) + 760 (шум).

| Тест | Результат |
|---|---|
| **B. специфичность** (чистый шум) | ✅ отсев по 4 барьерам |
| **C1/C3. мелкий стоп** | ✅ отсев (6 барьеров каждая) |
| **C2/C4. шесть фильтров** | ✅ отсев (7 барьеров каждая) |
| ℹ️ A. честный пробой | 5/7 — справочно, не критерий на реальных данных |

Напоминание, почему A не критерий на реальных данных: смысл теста
чувствительности в том, что эдж в данных заведомо ЕСТЬ. На рынке этого никто
не знает, поэтому «не прошла» одинаково объясняется и глухим фильтром, и
отсутствием эджа. Ground truth есть только у синтетики.

---

## Этап 5 — Широкий поиск на H4

**Воронка:**

```
59 гипотез в пуле  (11 базовых + 48 от Evolution)
    -> 4 отсеяны Research Score / Critic (все — дубликаты по отпечатку логики)
    -> 51 прогнана через все 7 барьеров
    -> 0 прошли все 7
    -> 0 достойных
```

**Распределение по уровню прохождения:**

| барьеров | гипотез |
|---|---|
| 6/7 | 3 |
| 5/7 | 7 |
| 4/7 | 18 |
| 3/7 | 4 |
| 2/7 | 3 |
| 1/7 | 16 |

**Накопленное знание — какие ТИПЫ идей доживают дальше:**

| тип | всего | тестировано | выжило | ср. барьеров | ср. робастность |
|---|---|---|---|---|---|
| **breakout** | 16 | 14 | 0 | **4.79** | **0.675** |
| trend | 1 | 1 | 0 | 4.00 | 0.498 |
| **momentum** | 9 | 9 | 0 | **3.89** | **0.632** |
| SMC | 17 | 15 | 0 | 2.60 | 0.439 |
| mean-reversion | 12 | 12 | 0 | 1.17 | 0.197 |

Это и есть тот актив, ради которого база знаний заводилась. Порядок устойчив
и осмыслен: **пробой > моментум > SMC > возврат к среднему**. Возврат к среднему
на H4 в этой корзине мёртв (1.17 барьера в среднем из 7). SMC-семейство
(order block, FVG, ретест, дивергенция) заметно слабее пробоя — при том, что
ретест уровня отдельно дотянул до 5/7.

**Где чаще всего умирают:**

| раз | барьер |
|---|---|
| 51 | 5. Поправка на multiple testing |
| 44 | 2. Walk-forward |
| 41 | 1. Out-of-sample |
| 23 | 6. Стабильность параметров |
| 21 | 7. Реалистичные издержки |
| 18 | 3. Кросс-инструмент |

---

## Этап 5б — Поиск на D1 (по запросу)

Данные собраны из H4 (`resample_ohlcv`), выравнивание по 00:00 UTC проверено,
агрегация сверена поштучно. 2177–3258 баров на инструмент, 2017–2026.

| Инструмент | Баров D1 | Период |
|---|---|---|
| BTCUSDT | 3257 | 2017-08-18 … 2026-07-28 |
| ETHUSDT | 3258 | 2017-08-18 … 2026-07-28 |
| BNBUSDT | 3179 | 2017-11-06 … 2026-07-28 |
| DOGEUSDT | 2578 | 2019-07-06 … 2026-07-28 |
| SOLUSDT | 2177 | 2020-08-12 … 2026-07-28 |

Калибровка на D1 пройдена (B ✅, C ✅).

### Пришлось адаптировать окно walk-forward — выношу отдельно

На D1 окно train в 365 дней даёт **8–11 сделок** на BTC при минимуме отбора
в 30. Значит КАЖДЫЙ внутренний перебор вернул бы `-inf`, подбор молча
откатился бы к дефолтному конфигу, и барьер walk-forward перестал бы что-либо
проверять — оставаясь при этом внешне нормальным.

Сделано два действия:
1. окно для D1 расширено до **train 1095д / test 365д** (5 окон вместо
   требуемых ≥4, ~30 сделок на подбор). Это параметр ПРОЦЕДУРЫ. Все критерии
   прохождения (≥60% окон в плюсе, суммарный OOS > 0, ≥4 окон) — без изменений;
2. вырождение подбора теперь **видно**: если в окне не набралось сделок,
   это пишется в отчёт барьера, и при вырождении больше половины окон барьер
   не засчитывается.

### Результат D1: хуже, чем H4

```
59 гипотез -> 51 в бэктест -> лучший результат 5/7 -> 0 достойных
```

| | H4 | D1 |
|---|---|---|
| Лучший результат | **6/7** (3 кандидата) | 5/7 |
| breakout, ср. барьеров | **4.79** | 4.50 |
| SMC, ср. барьеров | 2.60 | 3.00 |
| mean-reversion, ср. барьеров | 1.17 | 1.29 |
| Провалов барьера 4 (мин. сделок) | 0 | **12** |
| Медиана R у лидеров | положительная | **отрицательная почти у всех** |

Гипотеза, ради которой D1 и проверялся (меньше сделок → слабее давление
издержек), **не подтвердилась**. Издержки действительно давят слабее, но
выигрыш съеден падением выборки: 200–400 сделок по всей корзине вместо
1100–1500 на H4, и OOS-часть перестаёт быть значимой.

Плюс на D1 массово срабатывает флаг концентрации: у большинства лидеров
ожидаемость держится на 3–5 сделках, а медиана отрицательна. На H4 такого
почти нет — там распределение ровнее.

**Затухание эджа на D1 то же самое:**

| гипотеза (D1) | 2017–21 | 2022–26 | p(спад случаен) |
|---|---|---|---|
| level_retest+filter_vol_regime | +1.29R (PF 3.17) | −0.02R (PF 0.98) | **0.0040** |
| order_block+exit_trailing | +1.69R (PF 3.03) | +0.00R (PF 1.00) | **0.0087** |
| level_retest+exit_structure | +2.98R (PF 7.35) | +0.28R (PF 1.61) | 0.0250 |
| keltner_breakout+filter_vol_regime | +1.13R (PF 3.40) | +0.32R (PF 1.58) | 0.0307 |
| keltner_breakout+filter_htf | +1.18R (PF 3.46) | +0.29R (PF 1.51) | 0.0513 |

Тот же вывод независимым путём: эдж был, эдж ушёл.

**Вердикт по D1: не лучше H4 и не хуже по сути — та же картина при худшей
статистике.** Приоритетным ТФ остаётся H4.

---

## Этап 6 — Новые классы механизмов (по запросу)

Пробой и возврат к среднему закрыты предыдущим прогоном. Здесь — структурно
другие механизмы: **волатильностный** и **межрыночный**.

### Что понадобилось в движке

**Волатильностные блоки** (3 новых входа):
- `squeeze_breakout` — ширина полос Боллинджера в нижнем перцентиле своего
  диапазона за lookback И ATR ниже своей средней → вход на закрытии за полосой.
  Условие на РЕЖИМ волатильности, а не на положение цены;
- `ttm_squeeze` — полосы Боллинджера внутри канала Кельтнера = сжатие; вход на
  ОТПУСКАНИИ сжатия, направление по знаку моментума. Два независимых измерителя
  волатильности вместо одного;
- `nr_expansion` — самый узкий бар за N периодов → стоп-заявки по обе стороны
  его диапазона. Без единого индикатора, переоптимизировать нечего.

**Межрыночные блоки** потребовали новой возможности: блок должен видеть ВТОРОЙ
инструмент. Реализовано через `df.attrs["refs"]` с выравниванием **по
таймстампу, а не по позиции** — у инструментов разная длина истории (SOL с 2020,
BTC с 2017), позиционное совмещение сдвинуло бы ряды на годы.

- `ref_momentum` — импульс BTC как сигнал на альте (спилловер);
- `ref_seesaw` — тот же импульс, но вход ПРОТИВ (академические «качели»);
- `ref_trend` — фильтр: брать сигнал альта только по направлению тренда BTC.

BTC исключён из ведомых, подбор параметров перенесён на ETH: «импульс BTC как
сигнал на BTC» — это просто моментум, уже проверенный.

**Причинность проверена**: все пять новых блоков проходят тест на обрезку ряда
**бит в бит**, включая межрыночный, которому при обрезке подсовывается ПОЛНЫЙ
ведущий ряд — ровно как в бою, где BTC длиннее альта. Если бы блок выравнивал
по позиции, тест бы это поймал.

### Ещё одна поломка базы знаний, найденная по дороге

Базовый `order_block` исчез из базы, остались только его потомки по Evolution.
Причина: у дубликата тот же отпечаток логики, а ключ таблицы — (отпечаток, ТФ),
поэтому наивный upsert **подменял протестированную запись непротестированной** —
имя, метрики и вердикт оригинала стирались.

Исправлено: дубликат больше не затирает оригинал, а лишь помечается в его
`critic_objections`. База пересобрана начисто. Счётчик проб при пересборке
тоже обнулён — повторный прогон тех же гипотез на тех же данных не создаёт
нового смещения отбора, его надо считать один раз.

### Результат

```
104 гипотезы на H4 (все классы + Evolution) -> 0 достойных, лучший 6/7
104 гипотезы на D1                          -> 0 достойных
конфигов: 88 039 всего / 4478 в пуле отбора
```

Шесть кандидатов на 6/7, **все падают только на барьере 5**:

| кандидат | ТФ | класс | роб. | n | exp | DSR |
|---|---|---|---|---|---|---|
| xmkt_donchian_btc_filter+stop_structure | 4h | межрынок | **0.817** | 933 | +0.095R | — |
| xmkt_donchian_btc_filter+stop_channel | 4h | межрынок | 0.797 | 933 | +0.095R | — |
| keltner_breakout+stop_channel | 4h | пробой | 0.762 | 1471 | +0.142R | — |
| donchian_breakout+filter_htf | 4h | пробой | 0.761 | 1114 | +0.424R | — |
| keltner_breakout+stop_structure | 4h | пробой | 0.751 | 1471 | +0.140R | — |
| vol_ttm_squeeze | 4h | волатильность | 0.661 | 1431 | +0.206R | — |

Все шесть проходят режимы И свежесть. Барьер 5 — единственное, что стоит между
ними и статусом «достойная». И это не формальность: при 2239 пробах отбора
наблюдённый OOS-Sharpe неотличим от того, что даёт сам факт перебора.

---

## Бумажный форвард пробоя (модуль 6)

`donchian_breakout+filter_htf` заморожен в текущем конфиге.
**Боевого допуска нет** — фильтр не пройден, и это не изменилось.

```bash
python run_forward.py freeze --data ./data --tf 4h --name donchian_breakout+filter_htf
python run_forward.py status --data ./data     # по мере появления новых баров
```

Файл наблюдения: `forward/donchian_breakout_filter_htf_4h.json`.
Параметры заморожены навсегда: подкрутить их по ходу = превратить наблюдение
в подгонку в динамике (р.6.6).

**Пороги деградации — из Монте-Карло этой стратегии** (20 000 путей по 200
сделок, бутстреп из её же распределения R), а не из головы:

| метрика | медиана | 🟡 95% | 🔴 99% |
|---|---|---|---|
| просадка | 16.2R | **30.2R** | **39.0R** |
| серия убытков подряд | **10** | 16 | 20 |

Обрати внимание на нижнюю строку. При winrate 34% **медианная** серия убытков
за 200 сделок — **десять подряд**. Порог «5 убытков — выключаем» отключал бы
эту стратегию постоянно, в статистически ожидаемые моменты. Ровно то, о чём
предупреждает р.6.6.

Триггеры: 🟡 просадка >95-го перцентиля ЛИБО катящееся ожидание <0 на ≥30
сделках; 🔴 просадка >99-го перцентиля ЛИБО устойчивый минус на ≥50 сделках.

Сейчас: `🟢 нет данных` — данные заканчиваются датой заморозки, наблюдение
начнётся с первых новых баров.

---

## Funding: тракт готов, данных нет, влияние измерено

Скачать не удалось — биржевые хосты по-прежнему закрыты политикой сети
(`fapi.binance.com`, `api.bybit.com`, `data.binance.vision` → 403).

Что сделано вместо этого:

**1. Тракт проверен сквозным тестом.** Подставил файл со ставкой 0.03%/8ч:
начисленный funding вырос с 2206$ до 6543$ — загрузка, выравнивание по
8-часовым границам и списание работают. Достаточно положить
`*_funding.csv` рядом с данными, код менять не нужно.

**2. Измерено, насколько это вообще важно** (стратегия под наблюдением,
вся корзина):

| ставка / 8ч | % годовых | exp, R | изменение |
|---|---|---|---|
| 0.000% | 0% | +0.378 | — |
| 0.010% (текущий дефолт) | 11% | +0.377 | −0.001 |
| 0.030% | 33% | +0.375 | −0.003 |
| 0.050% | 55% | +0.373 | −0.005 |
| 0.100% (экстремум) | 110% | +0.369 | **−0.010** |

**Вывод: funding здесь второстепенен.** Даже при экстремальных 110% годовых
эдж просядет на 0.010R — это 2.6% результата, тогда как комиссии съедают
21–23% валовой прибыли. Причина: стоп широкий (2.4 ATR ≈ 5% цены), поэтому
нотионал на единицу риска умеренный, а среднее удержание 4.2 суток даёт
всего ~12 начислений.

Для стратегий с ТЕСНЫМ стопом (order block, ~0.8 ATR) вклад был бы в 3 раза
больше — там нотионал на единицу риска втрое выше. Но такие стратегии и без
того отсеиваются комиссиями.

**Отсутствие funding-истории не влияет на выводы этого прогона.** Приносить её
стоит, но это уточнение третьего знака, а не блокер.

---

## Playbook: условия, при которых пробой работал (р.6.7)

Не «стратегия работает», а **при каких условиях она работала**. Это не допуск
к торговле — это описание находки.

### `donchian_breakout + filter_htf` (лучший по робастности, 6/7)

```
вход    пробой канала Дончиана, период 20 (H4), сигнал на пересечении
фильтр  тренд старшего ТФ (фактор 6 = D1), EMA 50
стоп    2.0 ATR(14)
выход   трейлинг 3.0 ATR(14), предохранитель 240 баров
риск    1% на сделку
```

| Условие | Значение |
|---|---|
| Работало на | BTC, ETH, BNB, SOL, DOGE — все 5 |
| Работало в режимах | все, КРОМЕ медвежки 2026 |
| Период силы | 2017–2021 (+0.70R, PF 2.33) |
| Период слабости | 2022–2026 (+0.16R, PF 1.27) |
| **Сейчас (2026)** | **−0.20R, отрицательно** |
| Всего сделок | 1114 |
| Winrate | 35.5% |
| PF (вся история) | 1.75 |
| Макс. серия убытков | 13 |
| Издержки | 21–23% валовой прибыли; переживает удвоение |
| Медиана стопа | 2.4 ATR (не подсвечный) |
| **Почему НЕ допущена** | DSR 0.684 < 0.95; эдж отрицателен в текущем режиме |

Профиль риска стоит увидеть отдельно: **макс. серия убытков 13** при winrate 35%.
Под CFT с его 10% статичной просадкой это означает, что 1% на сделку — верхняя
граница, а не рабочая точка (р.1: проваливают размером позиции).

### `keltner_breakout + stop_channel` / `+ stop_structure` (6/7)

Тот же вывод, более ровный профиль: winrate 39%, PF 1.62, макс. серия убытков 12,
1471 сделка. Спад с +0.278R до +0.076R (p=0.0023), в 2026 −0.05R.

---

## Итог: есть ли достойные стратегии

**Нет.** Ни на H4, ни на D1, ни в старых классах, ни в новых.

Шесть кандидатов подошли вплотную (6/7, режимы ✅, свежесть ✅) и все шестеро
остановлены барьером 5. И это не формальность: тот же вывод независимо
подтверждает измерение затухания — эдж 2025+ у них в 3–10 раз меньше
исторического.

Главный содержательный результат прогона отрицательный, но полезный:
**затухание оказалось свойством рынка, а не конкретного механизма.** Пробой,
сжатие волатильности и межрыночный лаг — три разные природы сигнала — падают
одинаково и статистически значимо (p от 0.0005 до 0.042). Значит перебор
«ещё одного класса» на этой корзине упрётся туда же.

---

## Что делать дальше (по приоритету)

1. **Не искать новый класс на той же корзине и тех же ТФ.** Три класса
   проверены, спад у всех значим и одинаков. Четвёртый даст то же самое, а
   планку барьера 5 поднимет всем.

2. **Сменить не механизм, а РЫНОК или ГОРИЗОНТ.** По р.6.7 тестировать эдж
   можно на других классах активов — робастность на разных рынках сильный
   сигнал, а исполняемый контур всё равно остаётся крипто-Bybit. Варианты:
   расширить корзину за пределы пяти мейджоров (там, где ликвидности меньше,
   эдж мог не выторговаться), либо уйти на горизонты, где мы ещё не были.

3. **Ждать данных форварда.** `donchian_breakout+filter_htf` заморожен.
   Первые же новые бары начнут отвечать на вопрос «эдж умер или спит» —
   и этот ответ нельзя получить никаким бэктестом.

4. **Принести funding-историю** — уточнение третьего знака (измерено, см. выше),
   но для полноты картины стоит.

5. **Если решишь всё же расширять пространство** — единственное направление,
   которое я бы защищал: межрыночный класс. Он дал лучшую робастность прогона
   (0.817) и второе место по типам. Но и он затухает (p=0.0048), так что
   ожидания стоит держать низкими.

## Открытые вопросы к трейдеру

1. **Куда двигать поиск.** Механизмы на этой корзине исчерпываются: три класса
   проверены, спад у всех один и значим. Что осмысленнее — расширять корзину за
   пределы пяти мейджоров (там, где ликвидности меньше, эдж мог не
   выторговаться), или считать вопрос «простые правила на крипте» закрытым?

2. **Барьер 5 — единственное, что осталось.** Шесть кандидатов стоят вплотную,
   всё остальное пройдено. Порог 0.95 я НЕ предлагаю трогать и не буду. Но если
   захочешь обсудить этот случай осознанно — это единственное место, где
   обсуждение имеет смысл, и говорить надо про то, сколько проб мы считаем
   честными, а не про то, какой порог приятнее.

3. **Бюджет ширины поиска.** Каждая новая гипотеза поднимает планку всем;
   сейчас 4478 проб в пуле отбора. Имеет смысл зафиксировать лимит
   («не больше N гипотез на направление»), чтобы не размывать шанс хорошим
   кандидатам будущих прогонов.

4. **Пересборка базы знаний.** Я обнулил её и счётчик проб при починке дефекта
   с дубликатами и прогнал все классы заново одним махом. Логика: повторный
   прогон тех же гипотез на тех же данных не создаёт нового смещения отбора,
   поэтому считать его надо один раз. Если считаешь иначе — скажи.

---

## Где лежат результаты

| Файл | Что внутри |
|---|---|
| `knowledge.db` | база знаний: 55 гипотез, вердикты, метрики, режимы, причины |
| `lab_all_4h.json` | полный отчёт по H4 (все классы): ранжирование, типы |
| `lab_all_1d.json` | то же по D1 |
| `forward/*.json` | замороженные стратегии под наблюдением + пороги Монте-Карло |
| `calibration_report.json` | калибровка на глубоком H4 |
| `lab_4h.log`, `lab_d1.log`, `calib_*.log` | логи (в git не кладутся) |

Полезные запросы к базе:

```sql
SELECT name, barriers_passed, robustness, reason FROM hypotheses
  WHERE tested=1 ORDER BY barriers_passed DESC, robustness DESC LIMIT 10;

SELECT strategy_type, COUNT(*), AVG(barriers_passed), AVG(robustness)
  FROM hypotheses WHERE tested=1 GROUP BY strategy_type;
```
